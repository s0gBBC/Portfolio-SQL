# 🔍 SQL: Анализ продаж и блокировок абонентов

**Production-запрос для анализа продаж, блокировок и резиденции клиентов в телеком-системе**

---

## 📊 Бизнес-задача запроса

**Анализ продаж мобильных номеров за ноябрь 2025 с дополнительной аналитикой:**

1. **Фильтрация продаж** — только актуальные продажи за ноябрь 2025
2. **Определение резидентов/нерезидентов** — по стране гражданства
3. **Выявление заблокированных номеров** — статус блокировки по специальным кодам
4. **Обогащение данных** — добавление информации о дилерах, регионах, тарифах, оборудовании
5. **Оптимизация производительности** — использование временных таблиц с компрессией

---

## 🏗️ Архитектура запроса
ОЧИСТКА (DROP) → Удаление старых временных таблиц

ФИЛЬТРАЦИЯ ПРОДАЖ → filtered_sales (продажи за ноябрь 2025)

ФИЛЬТРАЦИЯ АБОНЕНТОВ → nomm (активные абоненты)

БЛОКИРОВКИ → blocked_numbers (заблокированные номера)

ОБЪЕДИНЕНИЕ → nom_priz (итоговая таблица со всей аналитикой)

ФИНАЛЬНЫЙ ВЫБОР → SELECT DISTINCT (чистые данные для отчета)

text

---

## 🛠️ Ключевые SQL-техники в запросе

### 1. **Управление временными таблицами**
```sql
-- Безопасное удаление перед созданием
DROP TABLE IF EXISTS nom_priz;
DROP TABLE IF EXISTS type_doc;
DROP TABLE IF EXISTS nomm;
DROP TABLE IF EXISTS filtered_sales;
DROP TABLE IF EXISTS blocked_numbers;

-- Создание с оптимизацией (Greenplum/PostgreSQL)
CREATE TEMPORARY TABLE filtered_sales
WITH (appendonly=true, compresstype=ZSTD, compresslevel=1)
ON COMMIT PRESERVE ROWS AS (...);
```

### Что это дает:

### ✅ Предотвращение конфликтов имен

### ✅ Экономия памяти (автоочистка)

### ✅ Ускорение запросов (компрессия ZSTD)

### ✅ Изоляция сессии (ON COMMIT PRESERVE ROWS)

## 2. Оптимизация фильтрации данных
```sql
WHERE
    act.activation_dttm >= '2025-11-01'
    AND act.activation_dttm < '2025-12-01'  -- Правильный диапазон дат
    AND (act.mrgn_id = 1 OR act.mrgn_id = 3 OR act.mrgn_id = 4)  -- Фильтр по mrgn_id
    AND act.sale_channel <> 8
    AND act.sale_channel <> 10  -- Исключение каналов продаж
```

#### Оптимизация:

#### Использование индексов по датам

#### Предварительная фильтрация для JOIN

#### Исключение ненужных каналов продаж

## 3. Сложные JOIN с бизнес-логикой
```sql
-- 8 таблиц с разными типами соединений
INNER JOIN uat_v_base.da_app_acc_con_cust_lnk AS cust_lnk 
    ON act.regid = cust_lnk.regid AND act.app_n = cust_lnk.app_n
    
LEFT JOIN nomm AS abnt 
    ON abnt.regid = act.regid AND abnt.app_n = act.app_n
    
LEFT JOIN blocked_numbers AS blocked 
    ON blocked.regid = act.regid AND blocked.app_n = act.app_n
```
## Типы JOIN в запросе:

INNER JOIN — обязательные связи (клиенты)

LEFT JOIN — опциональные данные (абоненты, блокировки, оборудование)

SELF JOIN — альтернативные источники (sale_point)

## 4. Расчетные поля (CASE выражения)
```sql
-- Определение резидентности
CASE
    WHEN mg.country = 643.060 THEN 'rezident'
    WHEN mg.country = 643.067 THEN 'rezident'
    WHEN mg.country = 643.071 THEN 'rezident'
    WHEN mg.country IS NULL THEN 'rezident'
    ELSE 'nerezident'
END AS priznak,

-- Статус блокировки
CASE
    WHEN blocked.regid IS NOT NULL THEN 'заблокирован'
    ELSE 'активирован'
END AS status
```
### Бизнес-логика в SQL:

Коды стран для резидентов (643.060, 643.067, 643.071)

Обработка NULL как резидента

Статус блокировки по наличию записи

## 5. Работа с кодами и справочниками
```sql
-- Блокировки по специальным кодам
WHERE sb.code IN (32768, 32776, 32784, 32832, 32840, 32856, 33280)

-- Соединение со справочниками для human-readable названий
LEFT JOIN uat_v_base.sas_fmt_f99022c AS f922 ON f922."start" = equip.equipment_model
LEFT JOIN uat_v_base.sas_fmt_fsrgc AS region ON region.start = abnt.region
LEFT JOIN uat_v_base.sas_fmt_f8000c AS fc ON fc.start = abnt.tp
```
### Что это дает:

Преобразование ID в понятные названия

Единый стандарт именования

Легкость чтения отчета бизнес-пользователями

## 📈 Бизнес-метрики которые считает запрос
## 1. Продажи по каналам
```sql
sale_channel, sale_channel_name  -- Канал продаж (магазин, онлайн, партнер)
sale_point_code, agent_name      -- Конкретная точка продаж/агент
```
## 2. География и дилеры
```sql
region, region_name              -- Регион (13 регионов: 52-66)
dealer_name                      -- Юридическое название дилера
personal_manager, personal_manager_name  -- Персональный менеджер'
```
## 3. Абонентская аналитика
```sql
phone_num                        -- Номер телефона
activation, registration         -- Даты активации и регистрации
mnp_port_in_dttm                 -- Дата переноса номера (MNP)
tarif_plan                       -- Тарифный план
equipment_name                   -- Модель оборудования
```
## 4. Юридическая информация
```sql
priznak                          -- Резидент/нерезидент (для налогообложения)
document_type                    -- Тип документа (паспорт, права и т.д.)
country                          -- Код страны гражданства
```
## 5. Статусы и блокировки
sql
status                           -- активирован/заблокирован
## ⚡ Оптимизационные техники
## Использование временных таблиц
```sql
-- Вместо сложного подзапроса в FROM
CREATE TEMPORARY TABLE filtered_sales AS (...);
-- Дальше работаем с готовыми данными
```
### Преимущества:

Разбиение сложного запроса на этапы

Повторное использование отфильтрованных данных

Улучшение читаемости и отладки

### Компрессия данных
```sql
WITH (appendonly=true, compresstype=ZSTD, compresslevel=1)
```
### Для больших объемов данных:

Сжатие ZSTD — экономия дискового пространства

Append-only — оптимизация для аналитических запросов

Ускорение обработки за счет меньшего IO

## Предварительная фильтрация
```sql
-- Фильтрация ДО JOIN
WHERE abnt.region IN (52, 53, 57, 54, 55, 56, 59, 60, 61, 62, 63, 64, 66)
AND abnt.activation_date >= '2025-11-01'
```
### Эффект:

Меньше данных в JOIN

Быстрее выполнение

Меньше потребление памяти

# 🎯 Какие задачи решает этот SQL
## 1. Отчет для налоговой
Разделение клиентов на резидентов/нерезидентов

Подтверждение резидентства по кодам стран

## 2. Мониторинг блокировок
Выявление номеров с особыми блокировками

Отслеживание кодов блокировок (32768, 32776 и т.д.)

## 3. Анализ продаж
Эффективность каналов продаж

Распределение по регионам

Активность дилеров и агентов

## 4. Перенос номеров (MNP)
Отслеживание перенесенных номеров

Дата переноса (mnp_port_in_dttm)

## 5. Качество данных
Обогащение сырых данных human-readable названиями

Связь с CRM (персональные менеджеры)

Интеграция со справочниками

# 📊 Пример результата (структура)
|phone_num	|activation	|region_name	|dealer_name	|tarif_plan	|status	|priznak|
|----------|----------|----------|----------|----------|----------|----------|
79161234567	|2025-11-15|	Москва|	ООО "Дилер-1"|	"Безлимит"|	активирован	|rezident|
79031234568	|2025-11-20|	СПб	ИП |"Агент-2"	|"Супер"	|заблокирован|nerezident|
79261234569	|2025-11-25|	Казань	|АО "Партнер-3"	|"Бизнес"	|активирован|	rezident|

# 🛠️ Технические навыки продемонстрированные в запросе
## Работа с СУБД:
 Greenplum / PostgreSQL (синтаксис WITH, временные таблицы)

 Оптимизация для аналитических запросов

 Управление памятью и производительностью

## SQL продвинутого уровня:
Сложные JOIN (8+ таблиц с условиями)

Временные таблицы с параметрами хранения

CASE выражения с бизнес-логикой

Оконные функции (неявно через DISTINCT)

Работа с датами и интервалами

## Оптимизация:
Предварительная фильтрация данных

Использование компрессии

Разбиение сложных запросов на этапы

Эффективное использование индексов

## Бизнес-аналитика:
Преобразование технических кодов в бизнес-понятия

Интеграция с CRM и справочниками

Расчет резидентности для налоговой отчетности

Анализ блокировок и статусов

# 💼 Почему этот SQL важен для бизнеса?
### Это не просто запрос, а готовое аналитическое решение:

Автоматизация рутинных отчетов — вместо ручного сбора данных из 10+ таблиц

Юридическая значимость — корректное определение резидентности для налоговой

Мониторинг рисков — выявление заблокированных номеров и проблемных продаж

Качество данных — единая точка истины с обогащенными данными

Производительность — оптимизирован для работы с миллионами записей

### Результат: Единый отчет для налоговой, безопасности, продаж и маркетинга вместо 5 разных запросов.

## 📞 Контекст использования
Система: Аналитическая платформа телеком-оператора\
Объем данных: Миллионы записей о продажах и абонентах\
Частота запуска: Ежедневно/ежемесячно для отчетности\
Пользователи: отдел продаж\

Навыки: SQL Advanced, ETL, Data Modeling, Performance Optimization
